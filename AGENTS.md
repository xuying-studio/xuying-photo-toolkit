# AGENTS.md

## 1. 项目使命

本项目使用“Python 共享业务核心 + PySide6/Qt Quick QML 跨平台界面”重新开发“旭影工具箱”。

必须完整保留 v1.4.0 的现有功能和安全语义，再提升界面表现。不得以重构为理由删减撤回、预览、冲突拦截、备份、平台兼容或关键词快切能力。

## 2. 开始工作前

每个 Agent 在执行任务前必须：

1. 完整阅读本文件。
2. 完整阅读 `DEVELOPMENT_PLAN.md`。
3. 检查当前 Git 分支、状态和最近提交。
4. 明确当前任务属于哪个实施阶段。
5. 读取与任务直接相关的现有代码和测试，不凭记忆重写。

如果用户尚未明确说“开始实施”或指定具体阶段，只能做只读审计、计划细化和风险说明，不得开始大规模编码。

## 3. 参考项目保护

现有 v1.4.0 项目路径：

`/Users/nerophotographer/VS CODE/把代码封装成app-关键词快切内嵌版`

该目录是只读参考基线：

- 不得修改、删除、移动或格式化其中的文件。
- 不得在其中创建分支、提交、构建缓存或测试输出。
- 如需迁移代码，复制到本项目后再修改。
- 迁移前记录源文件路径、基线提交和对应测试。

## 4. 强制架构边界

依赖方向必须保持：

```text
QML View
   ↓
PySide6 ViewModel / Controller
   ↓
Application Use Case
   ↓
Pure Python Domain / Port
   ↑
Platform Adapter / Infrastructure
```

具体要求：

- `domain` 不得导入 PySide6、QML、Tkinter、操作系统 UI 或具体 OCR/编码库。
- `application` 只编排用例、进度、取消和错误，不直接操作 QML 控件。
- `presentation` 负责 QObject、Property、Signal、Slot 和 QAbstractListModel。
- QML 只负责显示、交互和动画，不写文件处理、XMP、OCR 匹配或导出业务逻辑。
- macOS/Windows 差异必须通过 Protocol/Adapter 隔离。
- 禁止继续扩展 Tkinter；新项目不得引入 Tkinter 页面。
- 独立功能必须能作为模块加载，不得依赖额外 App 窗口或通过 `open` 启动另一个应用。

## 5. 必须保留的功能

### 时间重命名

- 当前文件夹扫描，默认不递归。
- EXIF 拍摄时间优先，文件修改时间兜底。
- RAW、JPG/JPEG、两种 XMP 侧车命名同步。
- 保留扩展名大小写。
- 冲突时阻止执行。
- 两阶段改名和完整撤回。

### RAW/JPG 配对清理

- 大小写不敏感的同目录配对。
- 支持清理孤立 RAW 或孤立 JPG。
- 先扫描预览，确认后执行。
- macOS 使用废纸篓与安全恢复副本。
- Windows 使用系统回收站。
- 支持最近一次清理恢复且不覆盖同名文件。

### Adobe XMP 同步

- JPG → RAW 与 RAW → JPG 双向同步。
- 星标和颜色标签可以独立选择。
- RAW 只更新或创建侧车，不直接写 RAW。
- JPG 写入前完整备份。
- 批次失败自动回滚。
- 支持撤回最近一次同步。
- 兼容现有 XMP 属性/元素、`xmp:`/`xap:`、单双引号和 UTF-8/UTF-16 处理。

### 关键词快切

- PNG、JPG、JPEG、HEIC 导入。
- 图片与文件夹选择、拖放导入、去重和排序。
- 离线 OCR、精确/模糊候选、手动选择候选。
- 队列点击、滚动、移除和拖动排序。
- 目标框移动和缩放。
- 画面比例、分辨率、帧率和每张保持帧数设置。
- 处理后 PNG、逐帧 PNG、无声 MP4 导出。
- 取消、进度、错误提示和项目重置。
- 切换工具后保留当前项目状态。

### 外观与平台

- 保留七套皮肤：专业深黑、星雾深蓝、流光玻璃、模块拼盘、编辑部极简、极光空间、柔软三维。
- 不恢复透明度滑杆；主窗口始终可读。
- macOS 使用完整动画、转场和可用的原生磨砂。
- Windows 保持同布局和功能，默认减少动画、模糊和透明材质。
- 尊重系统“减少动态效果”设置。

## 6. 文件安全红线

- 所有批量修改必须先扫描、预览、确认，再执行。
- 不允许静默覆盖现有文件。
- 不允许永久删除用户照片。
- 不允许直接修改 RAW 原文件。
- 写入必须采用临时文件、原子替换或可回滚事务。
- 撤回记录必须在执行实际修改前落盘。
- 网络盘、移动盘、符号链接、大小写差异和同名冲突必须有测试。
- 不得使用真实用户照片、真实隐私路径或真实备份清单作为测试夹具。

## 7. 并发和界面规则

- OCR、扫描、导出、FFmpeg 和大量文件处理不得阻塞 Qt 主线程。
- 后台工作使用统一 TaskRunner，并通过 Signal 回到主线程更新状态。
- 所有任务必须支持取消或明确说明不可取消阶段。
- QML ListView/TableView 必须使用虚拟化模型，不得一次创建上千个复杂控件。
- 动画只能作用于视觉属性，不得改变业务状态或阻止点击。
- 所有点击目标、滚动区域、键盘焦点、禁用态和错误态都必须测试。
- 图片预览层不得越界拦截侧栏、队列或工具栏的点击。

## 8. 平台策略

### macOS

- 第一目标：Apple Silicon、macOS 13+。
- 原生能力放入 `platform/macos` 或 `native/macos`。
- Apple Vision OCR 通过服务桥接使用，不允许把完整 SwiftUI 界面重新嵌入 QML。
- 原生磨砂不得降低文字对比度或遮挡 QML 交互。
- 发布前验证 arm64、动态库依赖、签名、Gatekeeper 提示和安装包结构。

### Windows

- 第一目标：Windows 10/11 x64。
- 使用系统回收站，不在照片目录创建 macOS 式恢复副本。
- OCR 后端必须离线，并输出与共享 OCRResult 一致的数据结构。
- 默认使用 Reduced Motion 和实体背景。
- 发布前验证中文路径、长路径、DPI 缩放、SmartScreen 说明和便携版 ZIP。

## 9. 视觉系统规则

- 颜色、圆角、间距、阴影、字体和动画时长全部来自设计 Token。
- 七套皮肤共用组件和状态规则，不复制七套页面。
- macOS 和 Windows 共用布局；平台差异只由 EffectPolicy 和 MotionPolicy 控制。
- 照片和视频画布保持中性背景，不让主题颜色影响色彩判断。
- 正文、次要文字、按钮和禁用态必须达到可读对比度。
- 不使用无意义渐变、装饰光球或遮挡内容的持续动画。
- 中文界面优先使用系统中文字体，并处理文本溢出与高 DPI。

## 10. 测试要求

每次改动至少执行与任务相关的测试；阶段结束必须执行完整测试矩阵。

强制测试类别：

- Domain 单元测试。
- 用例和事务回滚测试。
- macOS/Windows Adapter 合同测试。
- ViewModel Signal/Property 测试。
- QML 组件与键盘导航测试。
- OCR 固定样本与模糊候选测试。
- PNG/帧序列/MP4 输出验证。
- 页面切换、换肤、窗口缩放和任务取消测试。
- 打包后启动、依赖、签名和安装包内容测试。
- 与 v1.4.0 的功能等价对照测试。

测试失败时不得通过删除断言、降低安全标准或跳过平台行为来“修复”。

## 11. 工作方式

- 搜索文件和代码优先使用 `rg` / `rg --files`。
- 修改文件优先使用小范围补丁，保留用户已有改动。
- 代码注释、提交说明和用户沟通优先使用中文。
- 每次代码修改后，用一两句中文说明改了什么、为什么这么改。
- 主 Agent 负责架构、关键决策和最终验收；仅将边界明确的小任务委派，并复核结果。
- 不得使用 `git reset --hard`、`git checkout --` 等破坏性命令。
- 不得擅自推送 GitHub、创建 Release、安装系统软件或修改系统权限。
- 不得把密钥、授权码、私人路径或测试照片写入仓库。

## 12. 阶段门禁

- 阶段 0 未完成：不得创建正式业务实现。
- 等价测试未建立：不得删除或替换旧逻辑。
- Shared Core 未稳定：不得大规模制作页面动画。
- 单个平台没有通过功能验收：不得宣称跨平台完成。
- 打包产物未实际启动：不得发布。
- Release 资产未核对名称、大小和下载链接：不得宣称发布完成。

## 13. 完成定义

只有同时满足以下条件，才能宣布重构完成：

1. 四个工具在 macOS 与 Windows 都通过功能等价验收。
2. 所有危险操作保持预览、确认、备份、回滚和撤回能力。
3. macOS 完整动画与 Windows Reduced Motion 均通过实机测试。
4. 七套皮肤覆盖全部页面和关键词快切。
5. 两个平台的安装包可以在干净环境启动。
6. 旧设置与撤回数据完成只读迁移验证。
7. README、使用说明、许可证和第三方依赖清单同步更新。
