# v1.4.0 功能等价矩阵

基线提交：`4a287ff57f43b2632861d86607ea0c088793743f`

本矩阵既是迁移清单，也是 v2 验收合同。`v2 验收`一列必须由 Characterization Test 或实机步骤证明，不能只靠界面存在来判定等价。

## 时间重命名

| ID | v1.4.0 行为 | 证据 | v2 验收 |
| --- | --- | --- | --- |
| RN-01 | 界面只扫描当前文件夹，不递归 | `gui.py:2112-2116` 显式传 `recursive=False` | 子文件夹内照片不进入预览 |
| RN-02 | 支持 JPG/JPEG 与 23 种 RAW，忽略隐藏文件 | `core.py:48-58, 213-230` | 大小写扩展名与隐藏项固定夹具 |
| RN-03 | 优先 EXIF `DateTimeOriginal`，失败用文件修改时间 | `core.py:232-259` | 有效、缺失、损坏 EXIF 三组输出一致 |
| RN-04 | 从原文件名提取数字，同目录同数字组成一组 | `core.py:291-340` | RAW/JPG 同组获得同一日期编号 |
| RN-05 | 目标名为 `DSCYY-MM-DD-00001.ext`，延续当天已有最大编号 | `core.py:316-363` | 已命名与未命名文件混合时编号不重复 |
| RN-06 | 保留扩展名原始大小写 | `core.py:350-356` | `.JPG`、`.ArW` 输出大小写不变 |
| RN-07 | RAW 的 `.xmp` 与 `.RAW.xmp` 两种侧车均同步改名 | `core.py:262-289, 355-377` | 单侧车、双侧车、大小写侧车均不丢失 |
| RN-08 | 目标已存在或多个源指向同一目标时阻止执行 | `core.py:391-428` | 执行前报冲突且目录字节不变 |
| RN-09 | 使用唯一临时名完成两阶段改名；中途失败尽力回滚 | `core.py:430-471` | 故障注入后无临时残留、源文件不丢失 |
| RN-10 | 每批保存 JSON 清单，可撤回最近一次改名 | `core.py:472-559` | 清单先于实际修改落盘；撤回不覆盖已有文件 |
| RN-11 | 改名后新增的 RAW 侧车会被发现并参与撤回 | `core.py:500-547` | 补建侧车后撤回的命名行为与旧版一致 |

## RAW/JPG 配对清理

| ID | v1.4.0 行为 | 证据 | v2 验收 |
| --- | --- | --- | --- |
| CL-01 | 同目录、同主文件名、大小写不敏感配对 | `core.py:562-626` | `A001.JPG` 与 `a001.ARW` 可配对；跨目录不可配对 |
| CL-02 | 可选择清理孤立 JPG/JPEG 或孤立 RAW | `core.py:574-626` | 两个方向分别输出正确目标列表 |
| CL-03 | 界面当前版本默认不递归 | `gui.py:2402-2407` | 子目录目标不进入当前页预览 |
| CL-04 | 必须先扫描预览，再由用户确认执行 | `gui.py:2359-2495` | 未持有有效预览计划时执行按钮不可用 |
| CL-05 | 不永久删除；使用系统废纸篓/回收站 | `core.py:629-729` | 操作后原路径消失且系统恢复路径可验证 |
| CL-06 | macOS 移入废纸篓前建立硬链接或复制恢复副本 | `core.py:650-708` | 硬链接失败时自动转复制；副本可恢复 |
| CL-07 | Windows 不在照片目录建立 macOS 式恢复副本 | `core.py:639-648` | Windows 照片目录无隐藏恢复目录 |
| CL-08 | `cleanup_undo.json` 记录原路径、时间、废纸篓信息、inode 与恢复副本 | `core.py:685-729` | Schema 字段与旧版兼容 |
| CL-09 | 恢复顺序：安全副本 → 可访问废纸篓 → 平台回退 | `core.py:1101-1207` | 每条路径单独故障注入 |
| CL-10 | 恢复绝不覆盖同名文件 | `core.py:1120-1138` | 目标已存在时跳过并保留记录 |
| CL-11 | 部分恢复时只保留未恢复记录；全部成功后删除清单 | `core.py:1209-1227` | 连续两次恢复不会重复已成功项 |

## Adobe XMP 同步

| ID | v1.4.0 行为 | 证据 | v2 验收 |
| --- | --- | --- | --- |
| XM-01 | 支持 `JPG → RAW` 与 `RAW → JPG` | `core.py:1471-1542` | 两方向同组夹具输出一致 |
| XM-02 | 星标与颜色标签可独立选择，至少选择一项 | `core.py:1481-1486` | 三种有效组合与全不选错误 |
| XM-03 | 仅同步有效 1–5 星标和非空 Label | `core.py:1499-1533` | 0、非法值、空标签不写入 |
| XM-04 | 兼容 `xmp:`/`xap:`、属性/元素、单双引号、UTF-8/UTF-16 | `core.py:62-91, 1230-1436` | 在受支持的标准夹具中保持其余字段；非标准结构进入拒绝/风险路径 |
| XM-05 | RAW 只更新或创建侧车，不写 RAW 原文件 | `core.py:1628-1660` | RAW 内容哈希执行前后相同 |
| XM-06 | JPG 更新标准 Adobe XMP APP1；无 XMP 时插入 | `core.py:1662-1711` | 不破坏 EXIF APP1 和 JPEG 图像数据 |
| XM-07 | JPG 用临时文件原子替换，写后再读校验 | `core.py:1694-1711` | 通过批次入口执行时，写入失败后目标恢复到备份字节 |
| XM-08 | 执行前完整备份 JPG 或 RAW 侧车 | `core.py:1714-1782` | manifest 与备份先于目标写入存在 |
| XM-09 | 批次任一目标失败则恢复整个批次 | `core.py:1785-1812` | 第二项失败时第一项回到原字节 |
| XM-10 | 可撤回最近一次未撤回同步；新建侧车在撤回时删除 | `core.py:1815-1844` | 原文件/侧车字节级恢复，`undone_at` 更新 |

## 关键词快切

| ID | v1.4.0 行为 | 证据 | v2 验收 |
| --- | --- | --- | --- |
| QC-01 | PNG、JPG、JPEG、HEIC 导入 | `ProjectStore.swift:197-250` | 四格式与大小写扩展名均可导入 |
| QC-02 | 支持图片、文件夹、拖放；过滤隐藏项、去重并排序 | `ProjectStore.swift:197-250`、`ContentView.swift` | 重复 URL 只保留一次，顺序稳定 |
| QC-03 | macOS 使用离线 Apple Vision accurate OCR，中英文 | `OCRService.swift:17-26` | 待验证：脱敏截图上的文字、框坐标方向、置信度字段结构一致 |
| QC-04 | 优先完全匹配，否则 Levenshtein 相似度 `>= 0.70` | `OCRService.swift:28-76` | 0.69/0.70 边界、大小写和空格夹具 |
| QC-05 | 保存全部候选并允许手动改选 | `ProjectStore.swift:270-283` | 改选后状态与导出框同步 |
| QC-06 | 队列支持点击、滚动、移除和拖动排序 | `ContentView.swift`、`ProjectStore.swift` | 21 项、1000 项虚拟化与键盘/鼠标输入测试 |
| QC-07 | 目标框可移动、缩放并限制在 0–1 范围，最小尺寸 0.01 | `Models.swift:3-28` | 越界拖动、极小尺寸被正确钳制 |
| QC-08 | 支持 9:16、16:9、1:1、4:5、3:4、自定义，含分辨率、帧率、每图帧数 | `Models.swift:44-123` | 每个预设、奇偶宽高和非法值验证 |
| QC-09 | 输出处理后 PNG、逐帧 PNG、无声 H.264 MP4 | `ExportService.swift:63-234` | 数量、尺寸、命名、帧率、时长、无音轨一致 |
| QC-10 | 支持 23.976/29.97/59.94 等小数帧率时间基 | `ExportService.swift:151-234`、`TimelineMathTests.swift` | `ffprobe` 验证平均/真实帧率与时长 |
| QC-11 | OCR 与导出显示进度并可取消 | `ProjectStore.swift:285-390` | 取消延迟、最终状态、可再次启动 |
| QC-12 | 失败通过消息与弹窗呈现；项目可重置 | `ProjectStore.swift:373-390`、`ContentView.swift:46-56` | 用户取消与真实错误必须区分 |
| QC-13 | 设置保存到 `KeywordAligner.ProjectSettings.v1`，可从旧 suite 迁移 | `ProjectStore.swift:62-93, 398-402` | 旧域只读导入，旧值不删除 |
| QC-14 | Bridge 持有单一 Store，显隐切换不销毁状态 | `KeywordAlignerBridge.swift:39-68` | 切换其它工具再返回，队列/候选/目标框不丢失 |
| QC-15 | 输出目录与文件名会过滤非法字符并避免同名覆盖 | `ExportService.swift:63-129` | 非法字符、同秒二次导出、现有文件夹 |

## 外观、平台与通用安全

| ID | v1.4.0 行为 | 证据 | v2 验收 |
| --- | --- | --- | --- |
| UI-01 | 七套皮肤：专业深黑、星雾深蓝、流光玻璃、模块拼盘、编辑部极简、极光空间、柔软三维 | `ContentView.swift:6-26`、`gui.py` 主题定义 | 全页面、弹窗、禁用态、错误态截图矩阵 |
| UI-02 | 主窗口无透明度滑杆并保持文字可读 | `gui.py`、`tests/test_gui.py` | 七主题对比度与实体/磨砂策略验证 |
| UI-03 | 页面切换时保留当前页、共享文件夹、窗口尺寸与快切项目状态 | `docs/使用说明.md:108`、Bridge 单 Store | 重启前后与页面切换分别测试，不混为一项 |
| PF-01 | 所有危险操作先预览、确认，再执行 | 三个 Tk 页面与核心计划对象 | ViewModel 状态机禁止绕过确认 |
| PF-02 | 长任务不在主线程运行，进度回到 UI 线程 | `gui.py` 的 `run_job`、Swift Task | QML TaskRunner 主线程阻塞与取消测试 |
| PF-03 | macOS/Windows 清理行为不同但返回相同领域结果 | `core.py:629-729, 1101-1227` | Adapter 合同测试 + 两个平台实机测试 |
